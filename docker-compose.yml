services:
  app:
    build: .
    container_name: time-roi-hub

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"

    command: npm run dev